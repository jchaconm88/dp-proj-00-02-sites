{
  "name": "dp-proj-00-04",
  "version": "1.0.0",
  "private": true,
  "description": "WordPress Web Agency Monorepo - Gestión de plantillas de diseño y temas WordPress para múltiples clientes",
  "workspaces": [
    "scripts",
    "front/**/wp-content/themes/*"
  ],
  "scripts": {
    "validate": "node scripts/validate-structure.js",
    "build": "tsc --project scripts/tsconfig.json",
    "test": "vitest run",
    "deploy": "node scripts/deploy-client.js"
  },
  "devDependencies": {
    "@fast-check/vitest": "^0.4.1",
    "fast-check": "^4.8.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "ssh2-sftp-client": "^12.0.1",
    "yaml": "^2.9.0"
  }
}
